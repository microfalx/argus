<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Standard readable log format: Date, Thread, Level, Logger Name, Message -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="net.microfalx.argus.report.ReportTest" level="OFF" additivity="false" />

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
